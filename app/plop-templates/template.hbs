import { App } from "vue";
import Hook{{titleCase name}} from "./src/{{kebabCase name}}.vue";
export default (app: App): void => {
  app.component(Hook{{titleCase name}}.name, Hook{{titleCase name}});
};

export { Hook{{titleCase name}} };