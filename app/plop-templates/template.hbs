import { App } from "vue";
import {{titleCase name}} from "./src/{{kebabCase name}}.vue";
export default (app: App): void => {
  app.component({{titleCase name}}.name, {{titleCase name}});
};

export { {{titleCase name}} };